kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: local-pvc
  namespace: default
spec:
  storageClassName: nfs-server
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
kind: Pod
apiVersion: v1
metadata:
  name: local-test-pod
spec:
  containers:
  - name: local-test-pod
    image: busybox
    command:
    - "/bin/sh"
    args:
    - "-c"
    - "touch /mnt/SUCCESS && ls /mnt"
    volumeMounts:
    - name: pvc
      mountPath: "/mnt"
  restartPolicy: "Never"
  volumes:
  - name: pvc
    persistentVolumeClaim:
      claimName: local-pvc
